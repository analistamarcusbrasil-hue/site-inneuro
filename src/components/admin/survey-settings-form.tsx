"use client";
import { useState } from "react";
export function SurveySettingsForm({ campaign }: { campaign: { title: string; description: string | null; status: string; settings: Record<string, unknown> } }) { const [busy,setBusy]=useState(false); const [message,setMessage]=useState(""); return <form onSubmit={async(e)=>{e.preventDefault();setBusy(true);const form=new FormData(e.currentTarget);const response=await fetch("/api/admin/pesquisas/configuracoes",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(Object.fromEntries(form))});setBusy(false);setMessage(response.ok?"Configurações salvas.":"Não foi possível salvar.");}} className="border-border-light max-w-3xl space-y-5 rounded-3xl border bg-white p-7"><label className="block text-sm font-bold">Título público<input name="title" defaultValue={campaign.title} className="border-border-light mt-2 min-h-11 w-full rounded-xl border px-3 font-normal"/></label><label className="block text-sm font-bold">Descrição pública<textarea name="description" defaultValue={campaign.description ?? ""} className="border-border-light mt-2 min-h-28 w-full rounded-xl border p-3 font-normal"/></label><label className="block text-sm font-bold">Status<select name="status" defaultValue={campaign.status} className="border-border-light mt-2 min-h-11 w-full rounded-xl border px-3 font-normal"><option value="ACTIVE">Ativa</option><option value="PAUSED">Pausada</option><option value="DRAFT">Rascunho</option><option value="ARCHIVED">Arquivada</option></select></label><label className="flex gap-3 text-sm font-bold"><input type="checkbox" name="contact_enabled" value="true" defaultChecked={campaign.settings.contact_enabled !== false}/>Permitir contato opcional no final</label><button disabled={busy} className="bg-brand min-h-11 rounded-full px-6 font-bold text-white disabled:opacity-50">{busy?"Salvando…":"Salvar configurações"}</button>{message?<p role="status" className="text-sm font-bold text-emerald-700">{message}</p>:null}</form> }
